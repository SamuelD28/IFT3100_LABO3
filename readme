COMMANDS

cmake . -B build -G "Ninja" && ninja -C build && ./build/labo_2.exe

 ninja -C build && ./build/app.exe

-lglfw3 -lGL -lX11 -lpthread -lXrandr -lXi -ldl

DEPENDENCIES
- ninja 
- cmake
- glfw

export DISPLAY=:0.0
startxwin &
clean: rm -rf build && 
build: cmake . -B build -G "Ninja"
compile: ninja -C build 
exec: ./build/app.exe

ninja -C build && ./build/app.exe

rm -rf build && cmake . -B build -G "Ninja" && ninja -C build && ./build/app.exe